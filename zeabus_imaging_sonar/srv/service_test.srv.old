# message ที่ client กับ service ใช้ในการ request กับ response อาจมีมากกว่า 1 ตัว

int16 request_no

Test_create_msg

# --- เป็น syntax ของ service ไว้คั่นระหว่าง msg ที่ใช้ในการ request กับ response 
--- # message ที่ service ส่งให้ client 
int16 responseNO
std_msgs/String response 
